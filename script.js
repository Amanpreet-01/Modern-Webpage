var swiper = new Swiper('.tst-swiper', {
    loop: true,
    navigation: {
        nextEl: '.ri-arrow-right-line',  // Corrected "nextE1" → "nextEl"
        prevEl: '.ri-arrow-left-line'   // Corrected "prevE1" → "prevEl"
    }
});
